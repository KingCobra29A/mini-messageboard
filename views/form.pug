extends layout

block content
  form(method='post' action='/new')
    p
      label(for='messageText') Message Content: 
      input#messageText(type='text' name='messageText')
    p
      label(for='messageUser') User Name: 
      input#messageUser(type='text' name='messageUser')  
    button(type='submit' value='submit') Add Message
